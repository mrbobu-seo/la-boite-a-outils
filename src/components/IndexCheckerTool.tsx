import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const IndexCheckerTool = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Index Checker & Indexation</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is where the Index Checker & Indexation tool will be implemented.</p>
        <p>Coming soon!</p>
      </CardContent>
    </Card>
  );
};

export default IndexCheckerTool;
